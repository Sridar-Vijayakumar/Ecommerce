# Leave unset for same-origin deployments and local development through Vite's proxy.
# Set this when the API is hosted on a different origin.
VITE_API_URL=https://api.example.com/api
VITE_API_ORIGIN=https://api.example.com
